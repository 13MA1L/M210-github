name: Deploy Auto-Verwaltung

on:
  push:
    branches:
      - master
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Frontend bauen
        run: |
          cd frontend
          npm ci
          npm run build